# Terraform state — never commit
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl

# Sensitive variable values
terraform.tfvars

# Crash logs
crash.log
crash.*.log

# Override files (used for local dev overrides)
override.tf
override.tf.json
*_override.tf
*_override.tf.json
